# Database configuration (PostgreSQL)
DATABASE_URL=postgresql://aziz@localhost:5432/postgres

# Redis configuration for background tasks
REDIS_URL=redis://localhost:6379/0

# OpenRouter API Key
# OPENROUTER_API_KEY is loaded from shell environment (already set)

# LLM model — using free tier (no credits needed)
LLM_MODEL=meta-llama/llama-3.3-70b-instruct:free

# Slack webhook URL for notifications when meetings are booked
SLACK_WEBHOOK_URL=
